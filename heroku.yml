build:
  docker:
    web: backend/Dockerfile

release:
  image: web
  command:
    - /backend/wait-for-postgres.sh postgresdb && python /backend/manage.py migrate && python /backend/manage.py collectstatic --noinput
