build:
  docker:
    web: backend/Dockerfile

release:
  image: web
  command:
    - echo "Listing files in /app/backend"
    - ls /app/backend
    - python /app/backend/manage.py migrate && python /app/backend/manage.py collectstatic --noinput
